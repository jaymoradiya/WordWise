import { LanguageTransformPipe } from './language-transform.pipe';

describe('LanguageTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
